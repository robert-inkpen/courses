# %%
# Check to see what items are pop in a list

import pandas as pd

ini_data = pd.DataFrame()


def popularity_check(word: str) -> None:

    print(f"{word}")


if __name__ == "__main__":
    popularity_check(word="run")

    # %%
